import Contact from "@/sections/Contact";

// Capitalize the function name to "ContactPage" or "Page"
const ContactPage = () => {
  return (
    <main>
      <Contact />
    </main>
  );
};

export default ContactPage;
